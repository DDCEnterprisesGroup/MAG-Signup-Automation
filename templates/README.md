# Workbook template directory

The release artifact for this directory is `MAG_Signup_Automation_Clean_Template.xlsx`. It must contain exactly the four documented worksheets and the v1.1 People columns. Run `npm run validate:template` before packaging a release.

The initialization command never substitutes an operational workbook and never overwrites an existing live workbook.
