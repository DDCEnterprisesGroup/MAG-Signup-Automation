{
  "name": "mag-human-reviewed-extension",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test --test-concurrency=1 tests/*.test.mjs",
    "test:browser": "node --test --test-concurrency=1 tests/browser.test.mjs",
    "check": "npm test"
  }
}
